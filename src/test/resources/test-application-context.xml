<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:cache="http://www.springframework.org/schema/cache"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xsi:schemaLocation="http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd
         http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
         http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
         http://www.springframework.org/schema/cache http://www.springframework.org/schema/cache/spring-cache.xsd
         http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd">

    <context:property-placeholder location="classpath:test-application.properties" />

    <!-- enabling @Controllers scanning - exclude non-REST infrastructure -->
    <context:component-scan base-package="com.mycompany.entapp.snowman">
        <context:include-filter type="annotation" expression="javax.ws.rs.Path"/>
        <!-- Exclude messaging, scheduling, cache, management, and health packages not needed for REST tests -->
        <context:exclude-filter type="regex" expression="com\.mycompany\.entapp\.snowman\.infrastructure\.scheduling\..*"/>
        <context:exclude-filter type="regex" expression="com\.mycompany\.entapp\.snowman\.infrastructure\.messaging\..*"/>
        <context:exclude-filter type="regex" expression="com\.mycompany\.entapp\.snowman\.infrastructure\.cache\..*"/>
        <context:exclude-filter type="regex" expression="com\.mycompany\.entapp\.snowman\.infrastructure\.management\..*"/>
        <context:exclude-filter type="regex" expression="com\.mycompany\.entapp\.snowman\.infrastructure\.db\.health\..*"/>
        <context:exclude-filter type="regex" expression="com\.mycompany\.entapp\.snowman\.application\.schedule\..*"/>
        <context:exclude-filter type="regex" expression="com\.mycompany\.entapp\.snowman\.application\.cache\..*"/>
        <context:exclude-filter type="regex" expression="com\.mycompany\.entapp\.snowman\.application\.healthcheck\..*"/>
    </context:component-scan>

    <context:annotation-config/>

    <mvc:annotation-driven validator="validator"/>

    <!-- properties source -->
    <bean id="messageSource"
          class="org.springframework.context.support.ResourceBundleMessageSource">
        <property name="basename" value="error"/>
    </bean>

    <!-- validator (for doing Bean Validation)-->
    <bean id="validator"
          class="org.springframework.validation.beanvalidation.LocalValidatorFactoryBean">
        <property name="validationMessageSource" ref="messageSource" />
    </bean>

    <!-- H2 DataSource -->
    <bean id="dataSource"
          class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="driverClassName" value="${jdbc.driverClassName}" />
        <property name="url" value="${jdbc.url}" />
        <property name="username" value="${jdbc.username}" />
        <property name="password" value="${jdbc.password}" />
    </bean>

    <!-- Hibernate SessionFactory for H2 (using hibernate5 module for Hibernate 5.4.x) -->
    <bean id="sessionFactory"
          class="org.springframework.orm.hibernate5.LocalSessionFactoryBean">
        <property name="dataSource" ref="dataSource" />
        <property name="packagesToScan" value="com.mycompany.entapp.snowman.domain.model" />
        <property name="hibernateProperties">
            <props>
                <prop key="hibernate.hbm2ddl.auto">${hibernate.hbm2ddl.auto}</prop>
                <prop key="hibernate.dialect">${hibernate.dialect}</prop>
                <prop key="hibernate.show_sql">${hibernate.show_sql}</prop>
            </props>
        </property>
    </bean>

    <bean id="transactionManager"
          class="org.springframework.orm.hibernate5.HibernateTransactionManager">
        <property name="sessionFactory" ref="sessionFactory" />
    </bean>

    <tx:annotation-driven transaction-manager="transactionManager"/>

    <bean id="persistenceExceptionTranslationPostProcessor"
          class="org.springframework.dao.annotation.PersistenceExceptionTranslationPostProcessor"/>

    <bean id="jdbcTemplate" class="org.springframework.jdbc.core.JdbcTemplate">
        <property name="dataSource" ref="dataSource"/>
    </bean>

    <!-- RestTemplate for ClientService -->
    <bean id="restTemplate" class="org.springframework.web.client.RestTemplate">
        <property name="messageConverters">
            <list>
                <bean class="org.springframework.http.converter.StringHttpMessageConverter"/>
                <bean class="org.springframework.http.converter.json.MappingJackson2HttpMessageConverter"/>
                <bean class="org.springframework.http.converter.xml.SourceHttpMessageConverter"/>
            </list>
        </property>
    </bean>

    <!-- Simple cache manager for tests (replaces EhCache) -->
    <cache:annotation-driven/>
    <bean id="cacheManager" class="org.springframework.cache.support.SimpleCacheManager">
        <property name="caches">
            <set>
                <bean class="org.springframework.cache.concurrent.ConcurrentMapCacheFactoryBean">
                    <property name="name" value="clientFindCache"/>
                </bean>
            </set>
        </property>
    </bean>

</beans>
